const fs = require('fs');
const path = require('path');

const maplayers = path.join(__dirname, "./data/maplayers.json");

function initMapLayers(layerQueryMap) {
    layerQueryMap.forEach((query, layer) => {
        
    });
}